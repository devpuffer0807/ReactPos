import React, { Component } from "react";

export default class ItemEmpty extends Component {
    render(){
        return(
            <div className="callendar-item empty">{""}</div>
        )
    }
}